/**
 * UI feature flags. The backend, routes and tests stay fully functional — these
 * only control whether a feature is surfaced in the navigation/UI.
 *
 * RAG Evaluation is complete and working, but not surfaced for now. Set this to
 * true to show the Evaluation section again (nav + "Add to evaluation" actions).
 */
export const SHOW_EVALUATION = false;
